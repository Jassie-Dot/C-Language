/*
    Constant indentifiers are names assigned to constants in programming, which represent fixed values that cannot be changed during the execulation of a program.  
*/
#include<iostream>
#include<stdio.h>
#include<string>

int main()
{
    
    const int MAX_SIZE = 100;
    // int MAX_SIZE=200; Can't be change the value
    std::cout<<MAX_SIZE;
}